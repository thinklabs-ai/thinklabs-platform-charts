# Local Secrets Values Template for orchestrator-api (Azure Production)
# NEVER commit this file to version control
# Copy this to values-prod-secrets.yaml locally and fill in actual values
# 
# Usage:
#   helm install orchestrator charts/orchestrator-api \
#     --namespace orchestrator \
#     -f charts/orchestrator-api/values-prod.yaml \
#     -f charts/orchestrator-api/values-prod-secrets.yaml

slackWebhook:
  enabled: true
  url: "https://hooks.slack.com/services/YOUR/WEBHOOK/URL"
