apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "thinklabs-orchestrator.fullname" . }}-sa
  namespace: {{ .Release.Namespace }}
  {{- if .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
  {{- end }}
