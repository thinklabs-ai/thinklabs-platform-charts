# Local Secrets Values Template for orchestrator-api
# NEVER commit this file to version control
# Copy this to values-dev-secrets.yaml locally and fill in actual values
# 
# Usage:
#   helm install orchestrator charts/orchestrator-api \
#     --namespace orchestrator \
#     -f charts/orchestrator-api/values-dev.yaml \
#     -f charts/orchestrator-api/values-dev-secrets.yaml

slackWebhook:
  enabled: true
  url: "https://hooks.slack.com/services/YOUR/WEBHOOK/URL"
