apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: orchestrator-api-servicemonitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: orchestrator-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scheme: http
  namespaceSelector:
    matchNames:
      - orchestrator
